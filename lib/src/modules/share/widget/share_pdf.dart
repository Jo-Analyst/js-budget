import 'package:flutter/material.dart';

class SharePdf extends StatelessWidget {
  const SharePdf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('PDF'));
  }
}
